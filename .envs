#!/bin/bash

# Environment variables used throughout the deployment files.

# Master node
export SWARMHOST="set me to private ip of the host"

# Other remote host(s)
export REMOTEHOST_ONE="set me to remotehost@remotehost_ip"
export REMOTEHOST_TWO="set me to remotehost@remotehost_ip"

# Other docker context(s)
export DOCKER_CTX_ONE="set me to the docker context name"
export DOCKER_CTX_TWO="set me to the docker context name"
